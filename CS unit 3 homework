CREATING PROGRAM THAT CHECKS IF A YEAR PROVIDED BY THE USER IS A LEAP YEAR.

#include <stdio.h>

int main() {
  int year= 0;
  printf("Enter random year numbers ex.2020\n");
  scanf("%d",&year);
  if (year % 4) {
    printf("&year is a leap year\n");
  }
  else{
    printf("&year is not a leap year\n");
  }
  
  return 0;
}
