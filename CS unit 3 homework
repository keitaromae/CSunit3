#include <stdio.h>

int main() {
  int rows =0, k, l;
  printf("Enter the year numbers\n");
  scanf("%d",&rows);
  for (k=1; k<=rows; k++){
    for (l=1; l<=k; l++){
      printf("%i", k);
        number++;
    }
    printf("\n");
  }
  //printf("Hello World\n");
  return 0;
}

-

#include <stdio.h>

int main() {
  int year= 0;
  printf("Enter random year numbers ex.2020\n");
  scanf("%d",&year);
  if (&year % 4) {
    printf("&year is a leap year\n");
  }
  else{
    printf("&year is not a leap year\n");
  }
  
  return 0;
}
